export const API_URL = '127.0.0.1:3003';

/*export function TOKEN_POST(body) {
  return {
    url: API_URL + '/users/auth',
    options: {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(body);
    },
  };
}*/
